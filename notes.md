

# 聚类系数
## 局部聚类系数
**计算方法** 对于节点i，考虑与i直接相连的节点，统计它们之间的连接数与最大可能的连接数的比值。例如i有A, B, C三个邻居，其中只有A与B相连，那么i的局部聚类系数为1/3。
## 全局聚类系数
**方法一** 3*网络中的三角形个数/网络中连通三元组的个数（连通三元组即三个节点之间至少有两条边） 
**方法二** 局部聚类系数的平均值

# 直径
## 定义
网络中两个节点之间的最短路径的最大值

# 度分布
## 定义
网络中节点度数k的分布

# 最大连通分量
## 定义
网络中包含节点数最多的连通子图（即其所有节点之间都存在一条路径）
